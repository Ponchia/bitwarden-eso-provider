apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bweso.fullname" . }}
  labels:
    {{- include "bweso.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "bweso.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "bweso.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "bweso.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automount }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: bitwarden-eso-provider
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            {{- if .Values.config.singleOriginUrl }}
            - name: BWESO_SINGLE_ORIGIN_URL
              value: {{ .Values.config.singleOriginUrl | quote }}
            {{- end }}
            {{- if .Values.config.identityUrl }}
            - name: BWESO_IDENTITY_URL
              value: {{ .Values.config.identityUrl | quote }}
            {{- end }}
            {{- if .Values.config.apiUrl }}
            - name: BWESO_API_URL
              value: {{ .Values.config.apiUrl | quote }}
            {{- end }}
            - name: BWESO_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "bweso.credentialsSecretName" . }}
                  key: {{ .Values.credentials.existingSecret.keys.clientId | quote }}
            - name: BWESO_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "bweso.credentialsSecretName" . }}
                  key: {{ .Values.credentials.existingSecret.keys.clientSecret | quote }}
            - name: BWESO_MASTER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "bweso.credentialsSecretName" . }}
                  key: {{ .Values.credentials.existingSecret.keys.masterPassword | quote }}
            - name: BWESO_CACHE_TTL_SECONDS
              value: {{ .Values.config.cacheTtlSeconds | quote }}
            - name: BWESO_DEVICE_IDENTIFIER
              value: {{ .Values.config.deviceIdentifier | quote }}
            - name: BWESO_DEVICE_NAME
              value: {{ .Values.config.deviceName | quote }}
            - name: BWESO_DEVICE_TYPE
              value: {{ .Values.config.deviceType | quote }}
            - name: RUST_LOG
              value: {{ .Values.logging.rustLog | quote }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
